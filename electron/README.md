# Aura Desktop (Electron)

Lightweight Electron wrapper that opens the Aura dashboard in a native window.

## Build locally

```bash
npm install
npm run build:pc       # Linux .tar.gz
npm run build:pc:win   # Windows .zip
npm run build:pc:mac   # macOS .zip
```

Output goes to `electron-release/`.
